# =============================================================================
# AI Platform Configuration - PRIMARY SOURCE OF TRUTH
# Generated by Script 1 on Sun Mar 30 11:54:00 UTC 2026
# Tenant: test-deployment
# =============================================================================

# WARNING: This file is the single source of truth for the entire platform.
# All scripts (0, 2, 3) source this file directly.
# DO NOT EDIT MANUALLY - Re-run Script 1 to regenerate.

# ── TENANT CONFIGURATION ─────────────────────────────────────────────────────
TENANT_ID=test-deployment
PREFIX=ai
BASE_DOMAIN=local

# ── PLATFORM SETTINGS ───────────────────────────────────────────────────────
PLATFORM_ARCH=x86_64
GPU_TYPE=cpu
TOTAL_RAM_GB=8
MNT_DISK_GB=50
HOST_MTU=1500
HOST_IP=172.31.2.211

# ── SERVICE ENABLEMENT ───────────────────────────────────────────────────────
CADDY_ENABLED=false
POSTGRES_ENABLED=true
REDIS_ENABLED=true
OLLAMA_ENABLED=true
LITELLM_ENABLED=true
BIFROST_ENABLED=true
OPEN_WEBUI_ENABLED=true
QDRANT_ENABLED=true
WEAVIATE_ENABLED=false
CHROMA_ENABLED=false
N8N_ENABLED=false
FLOWISE_ENABLED=false
SEARXNG_ENABLED=false
AUTHENTIK_ENABLED=false
GRAFANA_ENABLED=false
PROMETHEUS_ENABLED=false

# ── PORT CONFIGURATION ───────────────────────────────────────────────────────
OPEN_WEBUI_PORT=3000
LITELLM_PORT=4000
BIFROST_PORT=8000
OLLAMA_PORT=11434
POSTGRES_PORT=5432
REDIS_PORT=6379
QDRANT_PORT=6333

# ── DATABASE CONFIGURATION ───────────────────────────────────────────────────
POSTGRES_USER=aiplatform
POSTGRES_PASSWORD=testpass123
POSTGRES_DB=aiplatform

# ── LLM CONFIGURATION ───────────────────────────────────────────────────────
OLLAMA_MODELS=llama2
ENABLE_OPENAI=false
ENABLE_ANTHROPIC=false

# ── SECURITY CONFIGURATION ───────────────────────────────────────────────────
JWT_SECRET=test-jwt-secret-key-12345
ENCRYPTION_KEY=test-encryption-key-12345

# ── DERIVED PATHS ───────────────────────────────────────────────────────────
BASE_DIR=/mnt/test-deployment
CONFIG_DIR=/mnt/test-deployment/config
DOCKER_NETWORK=ai-test-deployment-network
DOCKER_SUBNET=172.20.0.0/16
