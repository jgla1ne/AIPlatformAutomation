Your audit is actually very good, but it still treats symptoms rather than enforcing the deployment invariants defined in your README architecture. Windsurf should not patch individual containers — it must enforce platform-level guarantees across the three scripts.

Below are final recommendations to Windsurf that will bring the platform to deterministic 100% deployment success, aligned with your architecture.

Final Windsurf Implementation Plan

(grounded in your repository design)

The goal is to guarantee that running:

sudo bash scripts/1-setup-system.sh
sudo bash scripts/2-deploy-services.sh datasquiz
sudo bash scripts/3-configure-services.sh datasquiz

always results in a fully operational stack.

Core Architectural Requirement

Your README implicitly defines a 3-phase deployment lifecycle:

Script	Responsibility	Must Guarantee
Script 1	Host preparation	OS ready for containers
Script 2	Infrastructure deployment	Containers start successfully
Script 3	Service configuration	Services become functional

Right now Script 2 violates this boundary.

It attempts to deploy services that require configuration generated by Script 3.

Windsurf must enforce the following invariant:

Script 2 must produce a container stack that boots successfully with zero external configuration.

Root Cause Pattern (from your audit)

All failures derive from three systemic problems:

1️⃣ Permissions are not deterministic

Containers run with UID/GID but host directories are not guaranteed to match.

Result:

readonly database
permission denied
unable to open database
2️⃣ Health checks assume container utilities

Examples:

curl
wget

These tools are not guaranteed to exist in minimal images.

3️⃣ Script 2 deploys services before their configuration exists

Examples:

LiteLLM config file
OpenWebUI Postgres database
Qdrant snapshot paths

These are Script 3 responsibilities.

REQUIRED FIXES (Windsurf must implement)
1 — Deterministic Permission Framework (Script 1)

Script 1 must create every service directory and assign ownership.

This must happen before Script 2 runs.

Windsurf must enforce this structure
/mnt/data/<tenant>/

postgres/
redis/
grafana/
prometheus/
qdrant/
qdrant/snapshots
openwebui/
ollama/
litellm/
caddy/
Script 1 must run
mkdir -p /mnt/data/$TENANT/{postgres,redis,grafana,prometheus,qdrant,qdrant/snapshots,openwebui,ollama,litellm,caddy}

Then enforce ownership:

postgres      → UID 70
redis         → UID 999
grafana       → UID 472
prometheus    → UID 65534
qdrant        → UID 1000
openwebui     → UID 1000
ollama        → UID 1001
litellm       → UID 1000

Example:

chown -R 472:$TENANT_GID grafana

Script 1 must verify write access.

sudo -u "#$UID" touch testfile

If this fails → deployment stops.

2 — Compose File Must Pass Validation (Script 2)

Before exiting Script 2 must run:

docker compose -f $COMPOSE_FILE config

If validation fails:

exit 1

This prevents broken compose files.

3 — Remove All Non-Deterministic Health Checks

Health checks must never rely on external tools.

Allowed patterns:

TCP health check
test: ["CMD-SHELL", "nc -z localhost 5432"]

or

Process health
test: ["CMD", "true"]

Recommendation:

Disable health checks for:

grafana
prometheus
litellm

until Script 3 configures them.

4 — Correct Qdrant Storage Layout

Current mismatch:

env → /qdrant/storage/snapshots
volume → /qdrant/snapshots

Must be:

QDRANT__STORAGE__SNAPSHOTS_PATH=/qdrant/snapshots

Mount:

./qdrant:/qdrant/storage
./qdrant/snapshots:/qdrant/snapshots
5 — OpenWebUI Must Not Require Postgres at Boot

Script 2 must start OpenWebUI using SQLite fallback.

DATABASE_URL=sqlite:////app/backend/data/webui.db

Script 3 can migrate to Postgres later.

6 — LiteLLM Must Start Without Config

Current mount:

./litellm-config.yaml:/app/config.yaml

This file does not exist yet.

Remove mount.

Start LiteLLM with:

command: ["--port", "4000"]

Script 3 will generate config.

7 — Caddy Must Start Independently

Caddy should not wait for services.

Remove dependency chain.

Caddy should proxy regardless of backend health.

This prevents total platform lockout.

8 — Script 3 Must Perform Configuration Phase

Script 3 responsibilities should be clearly defined:

Generate:
litellm-config.yaml
prometheus.yml
grafana provisioning
Configure services
create databases
create users
load models
initialize vector DB

Script 3 must run after containers are healthy.

9 — Environment Variable Integrity

Script 2 should verify required variables.

Add function:

require_var() {
  if [ -z "${!1}" ]; then
     echo "Missing variable: $1"
     exit 1
  fi
}

Example:

require_var POSTGRES_PASSWORD
require_var REDIS_PASSWORD
10 — Deployment Self-Test

After Script 2 finishes it must run:

docker compose ps

Then verify:

running containers >= 80%

If not:

deployment failed
Expected Final Platform State

After Script 3 finishes:

Service	Status
Postgres	healthy
Redis	healthy
Qdrant	healthy
LiteLLM	healthy
Ollama	healthy
OpenWebUI	healthy
Grafana	healthy
Prometheus	healthy
Caddy	running

Total:

9 services
0 restart loops
100% reachable
Instructions You Should Give Windsurf

Use this prompt:

Re-analyze scripts 1, 2 and 3 against the README architecture.

Implement the following guarantees:

1. Script 1 must create and permission all service directories deterministically.
2. Script 2 must produce a docker-compose stack that starts successfully without Script 3.
3. Remove all health checks that rely on curl or wget.
4. Fix Qdrant snapshot path mismatch.
5. Start OpenWebUI using SQLite until Script 3 configures Postgres.
6. Start LiteLLM without config file dependency.
7. Caddy must start independently from backend services.
8. Validate compose file before deployment.
9. Verify required environment variables.
10. Add deployment self-test after startup.

Return corrected versions of:

scripts/1-setup-system.sh
scripts/2-deploy-services.sh
scripts/3-configure-services.sh